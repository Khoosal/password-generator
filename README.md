# password-generator

This is my test repo.
This python file generates n amount of passwords based on a set of parameters

#### TO SET UP ####
1. Open password_gen.py
2. Line 7, edit the file path so that it leads you directly to the text file
   E.g. pf = 'C:\\Users\\Harshil\\Desktop\\python_coding\\password_generator\\pw.txt'
   Ensure:
   - Double backslashes are used
   - The textfile name is correct, pw is an example
3. Run the file
(By default, all passwords will utilise lower and uppercase letters)
